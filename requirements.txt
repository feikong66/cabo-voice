# CABO voice service — Gradio SDK (free HF Spaces tier)
# CPU-only torch is pulled from the PyTorch CPU index (kept first so the
# RVC deps below don't accidentally pull a CUDA build).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchaudio==2.3.1+cpu

# App / API
gradio
fastapi
uvicorn[standard]
edge-tts
imageio-ffmpeg

# RVC inference dependencies (infer_cli.py, infer_pack, vc_infer_pipeline, ...)
fairseq
librosa
soundfile
pyworld
praat-parselmouth
pyrubberband
matplotlib
numpy
scipy
numba
colorama
tensorboardX
ffmpeg-python
beautifulsoup4
resampy
pyyaml
