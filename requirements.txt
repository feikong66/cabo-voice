# CABO voice service — default (VOICE_MODE=tts).
#
# Deliberately tiny: Edge TTS is a WebSocket client, so there is no model to
# load and no torch. Installs in ~20s, image lands around 200MB, RSS ~80MB —
# comfortably inside a 512MB free tier.
#
# For the RVC character-voice mode install requirements-rvc.txt on top; that
# pulls torch and needs ~2GB RAM.

edge-tts>=7.0.0
fastapi
uvicorn[standard]
starlette
